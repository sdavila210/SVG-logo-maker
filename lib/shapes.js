//defines class name and uses constructor function to create an instance for type. 
class Shape {
    constructor(type) {
        this.type = type;
    }

    render() {
        return '';
    }
}

module.exports = Shape;